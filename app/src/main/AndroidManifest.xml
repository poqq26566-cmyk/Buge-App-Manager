<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.GET_PACKAGE_SIZE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- 一加 / OPPO / Realme (ColorOS / OxygenOS) 权限管理页 -->
        <package android:name="com.oplus.securitypermission" />
        <package android:name="com.coloros.securitypermission" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.color.safecenter" />
        <package android:name="com.oneplus.security" />

        <!-- 小米 / Redmi / POCO (MIUI / HyperOS) 权限管理页 -->
        <package android:name="com.miui.securitycenter" />

        <!-- 华为 / Honor (HarmonyOS / EMUI) 权限管理页 -->
        <package android:name="com.huawei.systemmanager" />

        <!-- vivo (OriginOS / FuntouchOS) 权限管理页 -->
        <package android:name="com.vivo.permissionmanager" />

        <!-- 魅族 (Flyme) 权限管理页 -->
        <package android:name="com.meizu.safe" />
    </queries>

    <application
        android:name=".util.AppGlobals"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BugeAppManager"
        android:persistent="false"
        android:requestLegacyExternalStorage="true"
        tools:ignore="ProtectedPermissions">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AppDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".ActivityDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:alwaysRetainTaskState="true" />

        <activity
            android:name=".LogViewerActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".AboutUsActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".AppearanceActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".RestoreAppsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".UpdateOptionsActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".OptionalPermissionsActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".CustomLabelsActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".LabelDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.buge.appmanager.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
